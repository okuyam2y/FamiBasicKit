# ROM・セーブデータは絶対にコミットしない（このリポジトリは道具と解説だけを配る）
*.nes
*.sav
*.fds
*.ss
*.bin
*.rom
*.unf

__pycache__/
*.pyc
.DS_Store

# テストが失敗したときに残す画面のコピー。名指しでなく総称にしてあるのは、この
# ファイルが公開されるため — 特定の成果物名を書くと、それを作る非公開のテストが
# 在ることまで公開側に出る
*.png
